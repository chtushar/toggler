version: "2"
sql:
  - engine: "postgresql"
    queries: "./sql"
    schema: "./migrations"
    gen:
      go:
        package: "queries"
        sql_package: "pgx/v4"
        out: "queries"
        emit_json_tags: true
